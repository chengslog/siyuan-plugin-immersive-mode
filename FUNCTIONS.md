# 收纳入口核对

依据：用户参考截图、本机已安装 `siyuan-plugin-drawer/index.js` 的入口定义，以及本机 SiYuan `resources/stage/build/desktop` 中的工具栏和布局代码。下面表示识别范围，不表示所有入口在任意工作空间都存在。

| 参考功能 | 真实节点标识 |
| --- | --- |
| 插件管理 | `barPlugins` |
| 命令面板 | `barCommand` |
| 全局搜索 | `barSearch` |
| 同步 | `barSync` |
| 外观模式 | `barMode` |
| 界面缩放 | `barZoom` |
| 更多与设置 | `barMore` |
| 文档树 | Dock `file` |
| 大纲 | Dock `outline` |
| 收集箱 | Dock `inbox` |
| 书签 | Dock `bookmark` |
| 标签 | Dock `tag` |
| 智能助手 | Dock `agentChat` |
| 关系图 | Dock `graph` |
| 全局关系图 | Dock `globalGraph` |
| 反链 | Dock `backlink` |

这 16 项在真实节点存在且可用时收纳；缺少标签的标准原生节点使用已核对的名称和图标。禁用项、已移除节点不显示，不模拟不存在的功能。

发现并修复：

- 思源在 100% 缩放时给 `barZoom` 加上 `fn__none`，但原生点击处理仍有效。此前统一排除该类导致漏掉“界面缩放”；现在仅对它放宽此项过滤，保留 disabled / hidden 等其他可用性检查，不改变源按钮的显示状态。
- 参考收纳插件会把插件顶栏节点移到自己的卡片里。此前仅扫描 `#toolbar` 会遗漏这些节点；现在同时读取已启用插件的 `topBarIcons`，并按稳定 key 去重。不会移动节点或修改另一个插件。
- Dock 扫描覆盖所有真实 `.dock__item[data-type]`，包括底部和被其他收纳器管理的入口。

截图中另两项：Things 按已启用插件的真实 Dock 收纳；本插件的“进入沉浸模式”按钮在普通模式保留，沉浸菜单不再重复列出，退出放在一级卡片底栏。因此不机械追求截图中的总数 18。

有意保留的补充项：工作空间 / 主菜单、可用的前进后退、顶部页签区、原生工具栏（仅手动展开）。页面工具默认显示在正文原位置，宿主设置由原生主菜单访问，不再另外生成两个重复入口。菜单末尾“插件设置”打开本插件设置。桌面端底栏窗口图标只代理原生按钮点击，不搬移或复制原节点，退出沉浸后仍使用宿主原生入口。

原“页面与设置”三项来源（已打开页面现改为顶部页签区）：

- 已打开页面：遍历原生 `.layout-tab-bar:not(.layout-tab-bar--readonly) > [data-id]` 生成备用列表，选择时调用原标签，不是从原菜单搬来的同名按钮。
- 页面工具：临时显示当前文档顶部已有 `.protyle-breadcrumb`，即面包屑 / 页面操作条。
- 思源设置：调用宿主 `openSetting(app)` 打开思源设置窗口，是本插件补充的快捷入口。

合并顶栏右端的“+”和页签切换是原生 `.layout-tab-bar--readonly` 下的 `[data-type="new"]` / `[data-type="more"]`。只对后者按真实标签溢出添加可恢复的隐藏类；不改原节点和点击处理，也不和“已打开页面”列表混为同一个入口。

卡片上方固定页签区，取消常用及星标。下方保持“左侧栏 / 上侧栏 / 右侧栏 / 下侧栏”的单列分组列表。上、下栏入口按原栏位从左到右排序，左、右栏入口按原栏位从上到下排序；隐藏前记录可测位置，不可测时回退原 DOM 顺序。每组显示实际入口数，无入口时隐藏，不显示“原生 / 插件 / 补充”来源标签。一级卡片宽 280px、二级及更深卡片统一宽 230px，字号 11.5px；Dock 行同步原生 active / activefocus 状态，悬停与底栏按钮统一柔和背景，取消彩色指示线。底栏四个按钮各为 26×26px 正方形，均匀分布，不在窗口右上角创建控件。一级使用二级相同的固定行高、按钮内边距与实色背景，卡片两侧额外留白为 12px。长子菜单向上避让底边并可滚动。菜单隐藏快捷键提示；同步固定名称并在悬停时复用宿主查询显示最新信息。旧收藏 key 仅兼容保留。

主菜单、插件管理等已知菜单悬停约 180ms 或点击触发原按钮，将实际产生的原生菜单定位到主卡片外侧，不复制、搬移子项，也不增加“打开”中转按钮。移到子卡片时保持展开，悬停后的首次点击不重复执行或收起。宿主继续处理菜单选项、禁用与勾选状态、嵌套菜单事件和关闭清理。文档树、智能体等 Dock 仍点击调用原逻辑。`data-menu` 是思源所有插件顶栏按钮的通用属性，不能据此自动执行；未知插件首次点击实际出现原生菜单后，才允许后续悬停展开。

位置以当前真实节点为准，而不是按功能名称写死。思源左右竖栏分别有三个 `.dock__items` 区：前两个属于对应侧栏，第三个属于底部面板。`LeftBottom` / `RightBottom` 是侧栏下半区，`BottomLeft` / `BottomRight` 才是底部面板。节点被移动到收纳器后，尝试宿主 Dock 归属及插件注册的 position，未知位置单列“其他 Dock（位置未识别）”。底部状态栏的纯状态文字不当作 Dock 功能。

Windows“保留顶部工具栏”打开时，卡片不显示上侧栏、手动展开入口、页签区及底部控制按钮，标签和工具共用 28px 一行；关闭时完全沉浸，整个顶部栏隐藏，上侧栏、页签区和底部控制回到卡片。进入前记录原生页签横向起点，保持位置并在空间不足时避让原生工具。窗口控制按钮保留在宿主顶栏，退出沉浸后恢复原生布局。浏览器 / Docker 不显示 Windows 控制条。第三方入口的异步测量或非 click 事件仍需实际验证。

桌面客户端在完全沉浸时将顶部现有 8px 留白声明为原生拖窗区，由宿主处理拖动，不额外增加可见顶栏。“插件设置”位于菜单列表末尾；退出沉浸位于一级卡片底栏，桌面端另加最小化、最大化 / 还原、关闭窗口；最大化状态跟随宿主 body--maximize，原节点不移动。浏览器 / Docker 不创建拖窗区及窗口控制按钮。鼠标移入悬浮球自动展开卡片，不抢编辑焦点，触摸仍点击打开。

最新调整：“页面与设置”分组及重复入口全部移除。“已打开页面”已合并进卡片顶部“页签”区，页面工具保持正文原布局，思源设置由原生主菜单访问。页签默认显示 5 个（设置范围 1–20），超出折叠，点击调用原标签，当前页签高亮。完整顶栏开启时不显示此备用导航。上文三项来源记录仅解释历史入口出处。

设置移除位置重置、紧急退出及思源设置快捷入口。资源自动进入默认关闭，开启后观察中心区新加入的真实资源页签；已有页签、编辑区更新、页签重排不触发，关闭设置及卸载会停止观察。手动退出取消待执行的自动进入。退出沉浸完整恢复样式及原生几何后，触发宿主 resize 布局刷新，使页签右端按原生窗口按钮重新留白；不调整系统窗口尺寸。
