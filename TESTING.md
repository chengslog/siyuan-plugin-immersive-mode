# 0.1.0 验证记录

验证日期：2026-08-31。严格区分模拟检查与真实客户端验收。

最新变更：一级卡片增加横向留白，移除来源文字；两级宽度仍为 280px / 230px，字号及行高不变。底栏四个控制按钮为 26×26px 正方形，均匀分布。设置移除三个多余入口，新增资源自动进入开关（默认关闭）；退出沉浸完成清理后触发宿主布局刷新，避免页签切换与窗口按钮留白过期。二级菜单避让、正文页面工具、同步提示及原生窗口逻辑保留。

## 自动化

语法检查、构建、65 项测试通过（本机直接调用 Node 执行对应步骤），覆盖：

- 保留顶栏实时隐藏卡片页签和底部控件，关闭设置后恢复；原生页签起点每次进入前采样，工具区域变化时避让，退出清理。
- 顶栏进入 / 退出使用同一原生提示，无 title 重复提示；紧凑设置可在普通与沉浸模式打开，禁用时移除专属样式并关闭设置窗口。

- 资源自动进入默认关闭、即时保存、只响应新增页签；忽略启动已有页签、编辑、移动及瞬时删除的页签；手动退出、关闭开关和卸载取消待执行进入。
- 退出触发布局刷新时，原生节点、父节点、样式与几何已恢复；重复退出不重复刷新。

- 20 次进入 / 退出，原生侧栏、编辑器节点不变，无残留样式、监听、观察器、浮层。
- 旧偏好兼容、球位置约束、拖球贴边、防误触、保存失败提示、默认普通界面、移动端不启用。
- 页签数量默认 5、范围 1–20、非法值校正、有效输入即时保存、重载；展开 / 收起、重新打开恢复数量、原页面点击及高亮。
- 页签新增 / 改名 / 选中 / 删除刷新，保留展开状态；空列表提示，正文编辑不重建卡片。
- 取消常用及星标后分组无重复；页签与功能区独立、底部退出保留。
- 保持单列列表，分组顺序左、上、右、下；上下栏组内依据原横向位置、左右栏组内依据原纵向位置排序，并验证隐藏后缓存与 DOM 回退。
- 一次点击触发真实原菜单，原节点及子项事件不变，菜单出现在主卡片外侧；覆盖异步菜单、Esc 返回主卡片、关闭/卸载清理，文档树与智能体 Dock 不经过二级卡片。
- 顶栏沉浸按钮截断原始 PointerEvent 冒泡，并在下一帧替换状态 SVG，兼容思源 3.8.2 顶栏委托处理器，避免 `querySelector` 返回 null 后读取 `classList`。
- 悬停展开原菜单，之后首次点击不重复调用，跨卡片后仍可执行原选项；触摸、按键拖动、短暂停留、退出后的待执行悬停均不误触；Dock 与未知插件命令保持点击触发。
- 重复分组移除但正文页面工具保留；控制 Dock 图标具备提示和可访问名称；桌面三平台模拟原生事件及实时最大化状态，20 次退出后控件仍在原父节点；浏览器 / Docker 不显示窗口按钮。
- Dock active / activefocus / 取消选中变化在打开的菜单中同步。
- 移到普通命令、Dock、另一个菜单、页签区及底部按钮时关闭旧子卡片；不触发普通命令。进入 / 退出采用指定 SVG，窗口控制与齿轮引用原生图标，最大化后图标同步切换为还原。
- Dock 真实 slot 和位置变化分组、未知位置不猜测、空组隐藏；7 个顶栏和 9 个 Dock、100% Zoom、移除 / 禁用节点、其他收纳器移走的插件入口。
- Windows 顶栏开关实时应用及保存；完全沉浸隐藏整个顶栏，上侧栏入口收进卡片；完整顶栏不重复收纳上侧栏。
- 默认 / 已最大化宿主状态下最大化与还原显示正确，原生节点、事件及宿主状态保持。Happy DOM 的动态祖先 class 匹配存在缓存限制，动态切换另在 Chromium 检查。
- 完全沉浸隐藏整个顶栏，桌面客户端仅有 8px 隐形 drag 区；保留顶栏模式为 28px，原生按钮及 drag / no-drag 样式仍由宿主提供。浏览器 / Docker / Linux / macOS 不启用 Windows 控制条。
- 面板四边均为 1px，并统一使用 12px 主题圆角，外围灰色横竖线不再于角落形成直角；中心包装层无重复边框，退出恢复宿主样式。
- `width: 0px` 的左右 Dock 和 `height: 0px` 的底部 Dock 不绘制边框或阴影，展开面板仍保留完整圆角描边。
- 原生标签父节点、内联样式和点击 / 右键 / 指针事件保持；分屏分配、尺寸变化、隐藏 / 删除分屏和清理。
- 标签溢出才显示切换按钮，扣除自身占位避免震荡；“+”和原生事件保留，退出恢复类名。
- 平铺安装包 8 个文件、可执行 CommonJS 入口检查。

## Chromium 模拟宿主页

本轮使用 Browser 技能检查模拟页面，不连接或修改用户笔记：

最新顶栏及设置检查：普通 / 沉浸模式的首个页签左侧实测同为 540.12px；开启保留顶栏后，卡片页签区和底部按钮区均为 0 个，关闭后恢复。进入图标为 14px，aria-label 随状态更新，无 title 属性。设置预览高约 355px，无大块底部空白，三个输入控件与对应标题在同一行。页签下边框和底栏上边框均为 0px。预览设置结构已对齐宿主 Setting 的 config-item / config-name / b3-label__text，尺寸仍需真实主题确认。

本轮实测一级滚动区横向留白为 12px，来源标签为 0；底栏四个按钮均为 26×26px，中心间距约 63.7px，悬停使用主题柔和背景。设置只显示三项；通过界面开启资源自动进入后，手动退出保持普通状态，点击原生新建文档后自动进入。原生窗口按钮重叠修复已验证清理与刷新顺序，仍需实际客户端确认宿主重算结果。

底部窗口操作已组成卡片内部的独立控制 Dock：一级/二级正文均为 11.5px、行高 16px、内边距 5px 7px；控制按钮 22×22px、图标 14px。Dock 宽度为卡片宽度减 24px，位于滚动正文之后并保持固定，不随正文滚动；窗口右上角无插件控件。自动化覆盖原生位置恢复、Dock 唯一实例、清理和交互表面。

以下为前一版右上角方案记录：

本次检查：一级 280×480px；二级宽 230px，各级菜单文字均为 11.5px、内边距 5px 7px。右上角控件区域约 110×26px，悬停前 opacity=0，点击命中下方原生内容而不是控件；悬停后显示四个按钮。菜单内有“插件设置”，不再有底部按钮条。自动化覆盖角落位置判定、拖动不误展开、移开/窗口失焦隐藏、退出清理，以及原生窗口事件和状态同步。

以下紧凑版为此前版本记录：

最新紧凑版检查：卡片 248×480px，底部高约 37px，五个按钮 26px、图标 15px；所有底部按钮默认透明，悬停使用相同背景，没有指示线或红绿底色。主菜单快捷键隐藏；“同步”悬停更新并展示原生模拟信息，名称保持不变，未触发同步操作。56 项测试包含快捷键原值不变、同步异步提示更新、不重复查询、不重建菜单和退出清理。

以下为此前尺寸与颜色调整的历史检查记录：

- 1280×720：菜单正文与二级菜单正文 12px，无“页面与设置”；页面工具为 static，保持文档流。
- 主菜单悬停自动展开外侧卡片，跨越间隙后点击原生选项执行成功；底部五个图标同一行，无重叠。
- 文档树悬停时整行颜色与模拟原生选中颜色一致（背景 rgb(50,130,100)、文字白色），内部按钮背景透明。
- 最大化按钮调用原模拟事件，重新打开为还原；还原后退出，原窗口控件仍在 windowControls 内同一行。浏览器模式的独立 Dock 仅有退出图标。

- 控制 Dock 最新结构：位于主菜单内部最下方，宽度约占卡片 91%，正文独立滚动；四个按钮均 22×22，图标 14×14，与菜单前置图标一致。插件设置行的悬停/选中轮廓与其他菜单行一致，设置页底部显示插件名和 `v` 前缀版本号。
- 本次复查：从主菜单移到搜索，旧子卡片关闭、aria-expanded 恢复 false，搜索行出现主题色指示线，未执行搜索；已选中 Dock 行悬停也有可见标记。
- 指定退出 SVG、原生齿轮与窗口图标在模拟页显示正常；图标通过宿主 symbol 引用，随当前图标集变化。

此前布局回归记录（窗口按钮描述仅适用于当时版本）：

- 1280×720 完全沉浸：整个顶栏隐藏，正文仅保留 8px 外侧留白并声明为 drag；原工具、插件按钮和窗口按钮不复制，原标签栏隐藏，活动页面工具默认显示。
- 左侧、正文、右侧面板 top=36、高 676、bottom=712；四边 1px，外侧留白 8px，圆角保留。浏览器模式仍使用 y=8。
- 宿主原生窗口状态及按钮保持不变；插件不复制或模拟最小化 / 最大化 / 还原 / 退出。仅验证宿主状态 CSS，不是操作系统窗口。
- 7 个页签默认显示 5 个，展开显示 7 个；数量改为 3 后显示 3 个和“展开其余页签（4）”。展开并切换“项目记录”调用原标签，重新打开卡片对应项高亮。
- Windows 完整顶栏的工具与标签行均 top=0、高 28，标签项 top=2、高 24；覆盖宿主 29px 最小高度，避免裁切。正文不多占一行，卡片上侧栏分组为 0。
- 420×280 小窗口：卡片高 264，页签区约 106、功能滚动区约 61、底部约 55，均在窗口内；页签自身滚动，退出可访问。临时浏览器尺寸覆盖已恢复。
- 较早回归已验证双分屏同一行、浮动 Dock 留白、原生“+”、切换按钮随标签数量显隐。最新 28px 浮动 Dock 与主题组合仍需真机验收。

## 尚待真实思源验收

本轮在 Chromium 本地模拟宿主页检查了单列列表、外侧原生菜单、禁用选项、嵌套菜单及文档树原事件。上述交互不等同于真实思源验收；已运行的思源未被主动重启。

- 未主动启动 / 重启思源，也未触发真实操作系统的最小化或关闭。
- Windows 非最大化窗口拖动、顶部提示与移动指针、最小化 / 最大化 / 还原 / 退出、高 DPI 命中范围。
- 当前主题、浮动 Dock、Things / 智能体业务页面、原生及第三方菜单层级和异步定位。
- 真正禁用 / 启用及重启后的宿主数据落盘与载入。

浏览器渲染和模拟 API 成功不代表以上系统级操作通过。

## 复现

执行 `npm run build` 和 `node scripts/preview.mjs`，打开 `http://127.0.0.1:4179`（浏览器 / Docker 模拟）或 `http://127.0.0.1:4179/?host=windows`（Windows 模拟）；追加 `&panes=2` 检查双分屏。预览设置不写磁盘，窗口按钮只模拟事件与 class。
