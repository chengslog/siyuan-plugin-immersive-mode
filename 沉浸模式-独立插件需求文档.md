# 沉浸模式：独立思源插件需求文档

版本：需求草案 v1 · 2026-08-31

本文整理产品需求，不表示功能已经实现。本次只形成文档，不创建新仓库、不修改现有插件。

## 1. 产品定位

插件名称：**沉浸模式**。

提供一个类似苹果窗口放大按钮的入口。用户点击后进入更简洁、充分利用窗口空间的思源界面，原先分散在外围的功能入口收纳到一个半透明悬浮小圆球中。

这是独立的沉浸插件，不再提供原插件的左右抽屉模式、抽屉位置设置或抽屉开合动画。

“类似苹果放大按钮”目前理解为进入沉浸的操作体验和图标意象，**不默认等同于操作系统全屏或窗口最大化**。是否同时改变窗口状态，见待确认项。

## 2. 已确认的核心需求

1. 正常界面中有一个明确的沉浸入口，点击进入沉浸模式。
2. 进入后，通过半透明悬浮小圆球访问被收纳的功能。
3. 小圆球展开的功能菜单分成 **常用功能**、**其他功能** 两部分。
4. 用户可以将指定功能设为常用功能。
5. Windows 客户端处于普通窗口状态时，进入沉浸后仍能移动窗口：鼠标移到窗口最上方出现移动提示，按住鼠标可拖动整个窗口。

沿用此前已经明确的要求：

- Dock 图标入口收纳，Things、文档树、大纲等实际侧栏内容保留在原来的位置。
- 沉浸时消除外围工具栏的实际占位，不能留下顶部空隙或空白侧栏。
- 展开菜单的功能名称可以直接点击，不限定只能点击图标。
- 设置和退出沉浸放在弹出菜单底部。

## 3. 主流程

**普通思源界面 → 点击沉浸按钮 → 进入沉浸 → 点击小圆球 → 选择常用/其他功能 → 使用功能或退出沉浸。**

### 普通状态

- 保持思源原有界面，不主动收纳工具。
- 提供沉浸入口。建议先放在思源顶栏，使用容易理解的放大/全屏样式图标，并提供“进入沉浸模式”提示。
- 不显示侧栏抽屉，也不要求先打开一个菜单才能进入沉浸。

### 沉浸状态

- 隐藏指定的外围工具区域并释放空间。
- 编辑区和用户已打开的侧栏保留原内容及交互。
- 显示悬浮小圆球，作为收纳功能的统一入口。
- 打开功能后继续保持沉浸，除非用户明确选择退出。

### 退出

- 点击菜单底部“退出沉浸”，恢复思源普通界面。
- 移除小圆球、临时提示及插件添加的布局样式。
- 恢复工具入口，不改变文档内容、光标位置、打开页面或用户主动选择的侧栏。
- 不跳转到旧插件的抽屉模式。

## 4. 哪些区域收纳，哪些保留

| 区域 | 沉浸后的行为 |
| --- | --- |
| 思源内部顶部工具栏 | 隐藏完整横条及其占位，相关功能提供收纳入口 |
| 文档标签页栏 | 隐藏，并提供切换已打开页面的替代入口 |
| 文档面包屑、页面操作入口 | 默认隐藏，通过“页面工具”等入口访问 |
| 底部状态栏 | 隐藏；是否保留重要状态提醒见待确认项 |
| 左右 Dock 图标栏 | 图标入口收进悬浮菜单 |
| Things、文档树、大纲等侧栏内容 | 保留在原位置，不统一折叠、不改成浮动侧栏 |
| 编辑器及插件工作页 | 保留原结构、内容和交互，利用释放出的空间 |
| 思源原生弹窗、搜索、右键菜单 | 按原有功能打开，不被沉浸布局遮挡 |
| 浏览器地址栏和操作系统任务栏 | 不属于普通沉浸布局的处理范围 |

这里的“收纳”主要指**功能入口**，不是把编辑器和侧栏内容塞进小圆球。若用户原本打开了侧栏，沉浸后仍然保留；并非强制只剩一页文档。

## 5. 悬浮球与功能菜单

### 悬浮球

- 外观：小圆球、半透明，能辨认和点击，不是完全不可见。
- 点击：展开或收起功能菜单。
- 建议延续已有交互：拖动位置、左右贴边、记住位置、悬停时提高可见度。
- 窗口缩放或切换屏幕后，应保持在可见范围内。
- 拖动结束不应误触发菜单；打开菜单后不应遮住退出和设置。

透明度、尺寸是否可调，以及是否需要闲置后进一步淡化，属于可选配置，不是当前必须增加的设置项。

### 菜单结构

```text
┌─────────────────────────┐
│ 常用功能                 │
│ 图标  功能名称      ★    │
│ 图标  功能名称      ★    │
│                         │
│ 其他功能                 │
│ 图标  功能名称      ☆    │
│ 图标  功能名称      ☆    │
│ ……可滚动……              │
├─────────────────────────┤
│ 设置          退出沉浸   │
└─────────────────────────┘
```

以上为结构示意，不是最终视觉设计。设置和退出属于固定操作区，不作为第三个功能分类。

### 常用功能管理

- 在其他功能上点击星标或“设为常用”，将其加入常用区。
- 支持取消常用，回到其他功能区。
- 建议同一功能只出现在一个分区，避免重复列表。
- 保存常用名单，关闭菜单或重启后不会丢失。
- 常用区为空时，提示“可在其他功能中添加常用”，不制造大片空白。
- 点击星标只修改分类，不同时打开对应功能。
- 插件被卸载或功能不可用时，不保留无法操作的死入口；功能重新可用时可恢复原常用偏好。

“常用”由用户指定，不默认做自动推荐或按使用次数自动调整。拖动排序和跨分区搜索可作为后续功能。

### 功能点击

- 图标和名称都能触发对应功能，常用区和其他区行为一致。
- 调用思源或原插件的真实功能，不重新实现一套搜索、设置或文档树。
- 打开侧栏时，侧栏在原生位置展开。
- 建议触发功能后收起菜单，让用户直接操作结果；设置、分类等菜单管理操作例外。

### 可复用的收纳范围

首版可参考原项目已有入口，不代表必须一次性兼容所有插件：

- 原生工具：全局搜索、命令面板、插件管理、同步、外观、缩放、更多与设置。
- 侧栏功能：文档树、大纲、收集箱、书签、标签、智能助手、关系图、全局关系图、反链。
- 导航补充：工作空间与主菜单、前进、后退、已打开页面、页面工具。
- 第三方插件：通过思源标准插件接口创建的顶栏和 Dock 入口。

原生功能与第三方插件可以进入同一个常用区，不再另外分出“插件功能”“思源功能”等并列区域。

## 6. Windows 窗口顶部拖动

### 用户要求的行为

适用场景：**Windows 思源桌面客户端 + 普通非最大化窗口 + 沉浸模式**。

1. 平时不显示完整标题栏，不占一整条固定高度。
2. 鼠标靠近窗口最上方时，出现“可拖动窗口”的明确提示，例如轻量拖动条和文字提示。
3. 在提示的可拖动区域按住鼠标左键，移动整个思源窗口。
4. 松开鼠标结束拖动，移开后收起提示。

### 必须满足的边界

- 拖动的是系统窗口，不是移动悬浮球或滚动页面。
- 鼠标悬停只提示，不自动拖动、最大化或切换模式。
- 拖动提示不产生永久顶部空隙，不挤动编辑器和侧栏。
- 不劫持文档选择、工具按钮点击或窗口边缘缩放。
- 浏览器网页版不能拿网页顶部区域移动整个浏览器窗口，不应显示一个无法兑现的系统拖动提示。
- 最大化、系统全屏与普通窗口要区分处理；不默认把“最大化时向下拖动还原”算作已经支持。
- 具体拖动实现需要在思源 Windows 客户端实测，不能仅凭网页版验证通过就判定完成。

建议同时评估：悬停区域是否提供最小化、最大化/还原、关闭按钮。隐藏原顶栏后，这些窗口操作也需要方便的访问方式，但目前尚未确认其呈现形式。

## 7. 恢复与兼容性要求

- 始终有可用的退出路径。建议保留 `Alt+Shift+I` 切换快捷键，避免悬浮球异常时无法退出。
- 退出、禁用、卸载或初始化失败时，清理沉浸样式、提示区域和事件监听，恢复工具入口。
- 不修改笔记内容，不关闭文档，不重建第三方插件的业务页面。
- 切换前后保留侧栏状态；用户在沉浸期间主动打开了其他侧栏时，退出不能抢占其位置。
- 浮层层级正确：不能挡住思源原生对话框、菜单和输入区域。
- 保留原插件的事件和功能语义；菜单定位依赖顶栏的插件需要单独验证。
- 先明确支持 Windows 桌面客户端和桌面浏览器端；移动端与其他操作系统不直接承诺首版支持。
- 独立插件使用独立 ID、样式前缀和设置存储。测试时避免同时启用旧插件的沉浸功能，两套界面控制可能相互干扰。

## 8. 首版范围与暂不做的内容

**首版核心**：沉浸按钮、布局切换、半透明悬浮球、常用/其他两区、常用管理、文字点击、底部设置与退出、Windows 普通窗口顶部拖动、可靠恢复。

**可后续补充**：功能搜索、拖动排序、悬浮球外观自定义、自定义快捷键、使用频次统计、更多平台适配。

**不继承旧项目的内容**：左右抽屉、抽屉位置、默认展开抽屉、抽屉开合动画，以及“抽屉模式/沉浸模式”的选择开关。

新插件只有普通状态和沉浸状态。普通状态就是原始思源界面，不是另一种收纳模式。

## 9. 目前容易遗漏、需要确定的事项

| 问题 | 为什么需要明确 | 建议默认方案，尚非已确认需求 |
| --- | --- | --- |
| 点击入口是否同时最大化或进入系统全屏？ | 影响窗口拖动和退出后的尺寸恢复 | 只进入窗口内沉浸，不自动改变系统窗口状态 |
| Windows 的最小化、还原、关闭放哪里？ | 完整顶栏隐藏后仍需管理窗口 | 顶部悬停区提供窗口操作，拖动区与按钮分开 |
| 重启后自动沉浸还是回到普通界面？ | 影响可发现性和异常恢复 | 首次及默认启动为普通状态，可选“记住上次状态” |
| 第一次进入时哪些功能算常用？ | 决定初始体验 | 不擅自代选；提供清晰的添加常用提示 |
| 被隐藏的状态提醒如何呈现？ | 同步失败、只读等重要状态不应完全失去提示 | 保留原生通知，必要时在球上显示轻量状态提示 |
| 菜单触发功能后是否自动关闭？ | 影响连续操作和工作区遮挡 | 打开功能后关闭，修改常用分类时保持展开 |
| 快捷键及异常退出方式？ | 球被遮挡或移出屏幕时要能恢复 | 提供固定切换快捷键和重置悬浮球位置 |
| 是否处理底部 Dock 和非标准插件入口？ | 不能把“收纳所有功能”理解为已经兼容全部界面 | 首版明确入口清单，未支持项保留安全访问路径 |

开始开发前最值得先确认的是前三项，其他项可以采用表中默认方案后再迭代。

## 10. 验收清单

- [ ] 从普通思源界面点击一次入口，即进入沉浸，不经过抽屉。
- [ ] 该隐藏的工具栏不再占空间；编辑区及保留侧栏顶端没有空隙。
- [ ] Things、文档树等侧栏内容不被一起收纳。
- [ ] 小圆球可见、可操作，菜单包含常用与其他两个功能分区。
- [ ] 添加/取消常用立即生效，重启后名单仍保留。
- [ ] 两个分区的名称和图标均可点击，收藏操作不误触发功能。
- [ ] 菜单滚动时，设置和退出仍可访问。
- [ ] Windows 普通窗口沉浸后，顶部悬停出现提示，并能实际拖动窗口。
- [ ] 顶部拖动与文档操作、窗口边缘缩放互不干扰；网页版不会出现无效的拖窗提示。
- [ ] 连续进入/退出不会产生空白 Dock、重复入口、重复悬浮球或残留提示。
- [ ] 退出或禁用插件后恢复普通界面，笔记和已打开页面不受影响。
- [ ] 缩放窗口或改变显示器后，悬浮球和退出按钮仍能找到。

本清单用于新项目验收，尚未执行，不应视为当前功能的测试结果。
